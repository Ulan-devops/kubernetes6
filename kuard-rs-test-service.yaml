apiVersion: v1
kind: Service
metadata:
  labels:
    run: kuard-rs-test-service
  name: kuard-rs-test-service
spec:
  selector:
    app: kuard-rs-test-pod
  ports:
  - name: web
    port: 8080
    protocol: TCP
    targetPort: 8080
